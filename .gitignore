# Local credentials — never commit
benchmark.conf
control.local.conf

# TLS material
ssl-certs/*.pem

# Kubeconfigs are cluster credentials — never commit
kubeconfig
kubeconfig-*
*.kubeconfig

# Results / logs
results/
logs/

# SSH keys — never commit
ankit-dbaas-benchmarking
ankit-dbaas-benchmarking.pub

# Python cruft
__pycache__/
*.pyc
.venv/

# Generated dashboards
*_pmm_dashboard.html

# OS cruft
.DS_Store
